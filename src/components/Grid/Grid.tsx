import React from "react";
import { SectionWrapper } from "./Grid.element";
import GridTable from "./GridTable";

const Grid = () => {
  return (
    <SectionWrapper>
      <GridTable />
    </SectionWrapper>
  );
};

export default Grid;
